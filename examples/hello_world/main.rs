/*
 *  fn is keyword that define a function. Here we are defining
 *  the main function that is always the first code that runs
 *  in every executable Rust program.
 */
fn main() {
    println!("Hello, world!"); // The ! means that println is a macro
    println!("Hello, world! 2") // ; is optional only if it's the last statement
}
